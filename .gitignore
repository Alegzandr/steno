# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Regenerated on demand by tools/make-test-clip.ps1.
fixtures/rtf-fr-*.wav
fixtures/carry-fr-*.wav

# Staged by src-tauri/build.rs from llama-cpp-sys-2 output. Build artefacts,
# not sources: 60 MB of DLLs that are rebuilt from the crate on any machine.
src-tauri/runtime/

# `CARGO_TARGET_DIR=target-cuda` resolves against the shell's working
# directory, not against src-tauri/, so running cargo from the repo root
# creates a second one here.
/target-cuda/
